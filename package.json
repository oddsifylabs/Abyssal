{
  "name": "abyssal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "cd apps/api && npm run build",
    "start": "cd apps/api && npm run start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "sprites": "cd assets/sprites && node generate-svgs.js && node build.js"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": "20.20.2"
  }
}
